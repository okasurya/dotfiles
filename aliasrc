alias notes="terminal_velocity"
alias tx="tmuxinator start"
alias vim="nvim"

# cheatsheets
cs() {
    cat ~/.dotfiles/cheatsheets/$1
}

# vpn
vpnfz() {
    sudo openvpn ~/works/payfazz/post-creds/ovpn/$1.ovpn
}
