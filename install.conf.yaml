- defaults:
    link:
      relink: true

- clean: ['~']

- create:
    - ~/.config/nvim

- link:
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.aliasrc: aliasrc
    ~/.config/nvim/init.vim: config/nvim/init.vim
    ~/.tmux.conf: tmux.conf
